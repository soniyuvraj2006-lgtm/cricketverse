import { AdminStubPage } from "@/components/admin/admin-stub-page";

export default function AdminBannersPage() {
  return (
    <AdminStubPage
      title="Banners"
      description="Homepage banner carousel — upload, reorder, and toggle."
    />
  );
}
