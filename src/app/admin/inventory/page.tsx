import { AdminStubPage } from "@/components/admin/admin-stub-page";

export default function AdminInventoryPage() {
  return (
    <AdminStubPage
      title="Inventory"
      description="Stock levels, low-stock alerts, and warehouse sync."
    />
  );
}
